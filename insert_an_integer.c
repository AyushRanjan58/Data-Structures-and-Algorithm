//Write a program to insert an integer (at the start, at the end, in between) in an array of N integers.
#include<stdio.h>
int main()
{
int n = 7;
int arr[n];
printf("the array is:");
for (int i = 0;i<7;i++){
    printf("%d ",arr[i]);
}
return 0;
}